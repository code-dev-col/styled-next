import styled from 'styled-components';

/**
 * * Estilo general para list item
 */
const ListItem = styled.li`
  line-height: 1.8rem;
  letter-spacing: -0.02rem;
  margin: 2rem 0;
  padding-left: 0.5rem;
`;

export default ListItem;

